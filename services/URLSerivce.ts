export class UrlDB {
  //TODO Falta criar uma interface para o user e terminar de fazer a classe.
  static createUrl(url: string, user: string) {
    let status;
    return status;
  }
  static getUrl(url: string) {}
  private static verifyUrl(url: string) {}
}
